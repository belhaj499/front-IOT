# Cold Chain Monitoring — Frontend (React + Vite + Tailwind)

## Install
```bash
npm install
```

## Run
```bash
npm run dev
```

Open: http://localhost:5173

## Notes
- The frontend expects the backend on http://localhost:8080
- Auth is a simple token stored in localStorage and sent as `X-Auth-Token`
